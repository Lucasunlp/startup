let editarCtrl = angular.module('editarCtrl',[]); 
		editarCtrl.controller('editarController',function($scope,localStorageService,$routeParams) {

		// console.log($scope.movie);	
		// $scope.edit = function(){
		// 		var array =localStorageService.get('localStorageService');
		// 		$scope.nombre = array[$routeParams.movie].nombre;
		// 		$scope.duracion = array[$routeParams.movie].duracion;
		// 		localStorageService.set("localStorageService",$scope.nombre);
				

		// 	}	




		
		});